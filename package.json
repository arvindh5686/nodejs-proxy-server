{
  "name": "proxy-server",
  "version": "1.0.0",
  "description": "creates a proxy server which re-routes to a different endpoint",
  "main": "index.js",
  "scripts": {
    "start": "nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "proxy",
    "server"
  ],
  "author": "Aravindh Balakrishnan",
  "license": "MIT",
  "dependencies": {
    "request": "^2.69.0",
    "yargs": "^4.2.0"
  }
}
